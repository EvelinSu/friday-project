import React from 'react';
import {SMainTitle, SPageWrapper} from "../styled";

const Profile = () => {
    return (
        <SPageWrapper>
            <SMainTitle>
                Profile
            </SMainTitle>
        </SPageWrapper>
    );
};

export default Profile;
