import React from 'react';
import {SPackCardWrapper} from "./styled";

const PackCard = () => {
    return (
        <SPackCardWrapper>
            Картошка
        </SPackCardWrapper>
    );
};

export default PackCard;
