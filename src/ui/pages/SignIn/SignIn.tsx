import React from 'react';
import {SMainTitle, SPageWrapper} from '../styled';

const SignIn = () => {
    return (
        <SPageWrapper>
            <SMainTitle>
                Sign in
            </SMainTitle>
        </SPageWrapper>
    );
};

export default SignIn;
