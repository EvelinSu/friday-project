import React from 'react';
import {SMainTitle, SPageWrapper} from '../styled';

const SignUp = () => {
    return (
        <SPageWrapper>
            <SMainTitle>
                Sign up
            </SMainTitle>
        </SPageWrapper>
    );
};

export default SignUp;
