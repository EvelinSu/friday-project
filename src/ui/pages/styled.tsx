import styled from "styled-components";


export const SPageWrapper = styled.div((props) => ({
    display: "flex",
    alignItems: "center",
    flexDirection: "column",
}))

export const SPageContent = styled.div(() => ({
    //
}))

export const SMainTitle = styled.div(() => ({
    fontSize: 26,
    padding: "20px 0 30px 0",
}))